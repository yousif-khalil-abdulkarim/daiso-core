import { describe, test } from "vitest";

describe("function: isDatabaseSharedLockAdapter", () => {
    test.todo("Should return true when given IDatabaseSharedLockAdapter");
    test.todo("Should return false when given ISharedLockAdapter");
});
