---
"@daiso-tech/core": minor
---

Removed `run`, `runBlocking` methods from `ILock` contract (lock component).

Removed `run`, `runBlocking` methods from `ISemaphore` contract (semaphore component).

Removed `runReader`, `runWriter`, `runReaderBlocking`, `runWriterBlocking` methods from `ISharedLock` contract (shared-lock component)
